# Copy to .env in apps/api/ if you want to override defaults.
# Do not put secrets in git.

APP_NAME=National Weather Analytics API
APP_VERSION=0.1.0
ENVIRONMENT=local
API_V1_PREFIX=/api/v1

# Reserved for a later phase (not used; no database connection in this phase):
# DATABASE_URL=postgresql://user:password@localhost:5432/weather
