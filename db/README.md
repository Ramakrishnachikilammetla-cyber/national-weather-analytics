# Database

PostgreSQL + PostGIS is the system of record (see `docs/DATABASE_DESIGN.md` and `docs/ARCHITECTURE.md`).

**This phase does not connect to a database.** Schema migrations will be added later (Alembic or equivalent) under `migrations/`.
